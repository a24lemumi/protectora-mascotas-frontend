## 1. Fix Register Endpoint

- [ ] 1.1 Change the register endpoint in `js/api.js` from `/api/register` to `/api/auth/register` (line ~50)
- [ ] 1.2 Verify the change matches the backend route defined in `public/index.php` line 22

## 2. Testing

- [ ] 2.1 Test registration form to confirm it calls the correct backend endpoint without 404 error
