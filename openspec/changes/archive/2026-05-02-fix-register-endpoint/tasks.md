## 1. Fix Register Endpoint

- [x] 1.1 Change the register endpoint in `js/api.js` from `/api/register` to `/api/auth/register` (line ~50)
- [x] 1.2 Verify the change matches the backend route defined in `public/index.php` line 22

## 2. Testing

- [x] 2.1 Test registration form to confirm it calls the correct backend endpoint without 404 error
